$kind: http-request
url: "{{finance_url}}/api/v1/households/{{household_id}}/cards"
method: POST
headers:
  Authorization: Bearer {{access_token}}
  Content-Type: application/json
body:
  type: text
  content: '{ "name": "Visa principal", "type": "OTHER", "openingBalance": { "amount": 3000, "currency": "USD" } }'
order: 12000
