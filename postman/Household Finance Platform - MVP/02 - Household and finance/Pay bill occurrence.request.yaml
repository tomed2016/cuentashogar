$kind: http-request
url: "{{finance_url}}/api/v1/households/{{household_id}}/bill-occurrences/{{occurrence_id}}/payments"
method: POST
headers:
  Authorization: Bearer {{access_token}}
  Content-Type: application/json
  Idempotency-Key: "{{payment_idempotency_key}}"
body:
  type: text
  content: '{ "accountId": "{{account_id}}", "description": "Pago de electricidad" }'
order: 16000
