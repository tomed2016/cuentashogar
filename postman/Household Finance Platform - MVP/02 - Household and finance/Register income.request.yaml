$kind: http-request
url: "{{finance_url}}/api/v1/households/{{household_id}}/transactions"
method: POST
headers:
  Authorization: Bearer {{access_token}}
  Content-Type: application/json
body:
  type: text
  content: '{ "accountId": "{{account_id}}", "categoryId": "{{category_id}}", "type": "INCOME", "amount": { "amount": 2500, "currency": "USD" }, "description": "Ingreso mensual" }'
order: 8000
