$kind: http-request
url: "{{identity_url}}/api/v1/users/me/password"
method: PUT
headers:
  Authorization: Bearer {{access_token}}
  Content-Type: application/json
body:
  type: text
  content: '{ "currentPassword": "{{password}}", "newPassword": "{{new_password}}" }'
order: 4000
